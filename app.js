const button = document.querySelector(".btn")
const first_player =document.querySelector(.first_player)
const secend_player =document.querySelector(.secend_player)

button.addEventListener("click",() =>{
const first_random_number  = Math.random(Math.random()*2)
const secend_random_number  = Math.random(Math.random()*2)
});